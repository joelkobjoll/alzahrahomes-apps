# ── Guest Web Environment ────────────────────────────────────────────
# Configure in Dokploy UI under the guest-web service environment tab.
# These are build-time args; Dokploy passes them during image build.

NODE_ENV=production
API_URL=https://api.alzahra.local
AUTH_URL=https://auth.alzahra.local
