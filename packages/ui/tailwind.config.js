/**
 * Tailwind CSS v4 configuration for Alzahra Homes design system.
 *
 * Uses the CSS-first configuration approach introduced in Tailwind v4.
 * Theme values are defined in src/styles.css via @theme.
 */
export default {
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
    '../../apps/*/src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
